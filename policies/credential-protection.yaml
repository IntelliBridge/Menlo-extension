policy:
  name: Credential Protection
  description: Prevent DoD credentials from being used on non-DoD sites

  match:
    site_domain_not:
      - ".mil"
      - ".gov"

  actions:
    inject_scripts:
      - credential-guard

  telemetry:
    credential_block: high
